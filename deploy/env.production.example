# Frontend env (production build)

# Public API base URL (no trailing slash).
# App Platform: https://your-api.ondigitalocean.app
# Same-host Droplet with nginx /api proxy: leave empty
VITE_API_URL=https://REPLACE_API_URL
